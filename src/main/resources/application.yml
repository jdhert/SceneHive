spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    driver-class-name: org.postgresql.Driver
    username: testuser
    password: testuser
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:your-email@gmail.com}
    password: ${MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9094}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false
    consumer:
      group-id: ${KAFKA_NOTIFICATION_CONSUMER_GROUP:scenehive-notification-service}
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      auto-offset-reset: earliest
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.value.default.type: com.example.auth.notification.contract.NotificationCommand
        spring.json.trusted.packages: com.example.auth.notification.contract
        spring.json.use.type.headers: false
    listener:
      ack-mode: record
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:placeholder-client-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:placeholder-client-secret}
            scope:
              - email
              - profile
          kakao:
            client-id: ${KAKAO_CLIENT_ID:placeholder-client-id}
            client-secret: ${KAKAO_CLIENT_SECRET:}
            client-authentication-method: ${KAKAO_CLIENT_AUTH_METHOD:none}
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Kakao
            scope:
              - account_email
              - profile_nickname
              - profile_image
          naver:
            client-id: ${NAVER_CLIENT_ID:placeholder-client-id}
            client-secret: ${NAVER_CLIENT_SECRET:}
            client-authentication-method: ${NAVER_CLIENT_AUTH_METHOD:client_secret_post}
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Naver
            scope:
              - name
              - email
              - profile_image
              - nickname
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

jwt:
  secret: eW91ci0yNTYtYml0LXNlY3JldC1rZXktaGVyZS1tdXN0LWJlLWF0LWxlYXN0LTMyLWNoYXJhY3RlcnMtbG9uZy1mb3Itc2VjdXJpdHk=
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

server:
  port: 8081

app:
  frontend-url: ${FRONTEND_URL:http://localhost:3000}
  async:
    mail:
      core-pool-size: 2
      max-pool-size: 4
      queue-capacity: 200
      keep-alive-seconds: 60
      thread-name-prefix: mail-async-
      retry:
        max-attempts: 5
        initial-delay-seconds: 5
        max-delay-seconds: 300
        backoff-multiplier: 2.0
        batch-size: 20
        poll-interval-ms: 3000
  kafka:
    notifications:
      enabled: ${KAFKA_NOTIFICATIONS_ENABLED:false}
      command-topic: ${KAFKA_NOTIFICATION_COMMAND_TOPIC:scenehive.notification.command.v1}
      retry-1m-topic: ${KAFKA_NOTIFICATION_RETRY_1M_TOPIC:scenehive.notification.command.retry.1m.v1}
      retry-5m-topic: ${KAFKA_NOTIFICATION_RETRY_5M_TOPIC:scenehive.notification.command.retry.5m.v1}
      dlq-topic: ${KAFKA_NOTIFICATION_DLQ_TOPIC:scenehive.notification.command.dlq.v1}
      consumer-group-id: ${KAFKA_NOTIFICATION_CONSUMER_GROUP:scenehive-notification-service}
  file-storage:
    upload-dir: ./uploads
    avatar-dir: avatars
    allowed-types: image/jpeg,image/png,image/gif,image/webp
    max-avatar-size: 5242880

# Actuator configuration (expose only health and info)
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized
  health:
    defaults:
      enabled: true
